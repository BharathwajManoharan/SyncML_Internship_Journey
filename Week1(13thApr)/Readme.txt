* download nltk_data from idle(Link: https://www.nltk.org/data.html).
* Create venv and activate it.
* Install Tensorflow,keras,nltk,pickle,flask.
* Run train.py and save the model.
* Run app.py (If it successively trained the model and saved the model in the same folder).
