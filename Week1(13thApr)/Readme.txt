1) download nltk_data from idle.
(Link: https://www.nltk.org/data.html )

2) Create venv and activate it.

3) Install Tensorflow,keras,nltk,pickle,flask.

4) Run train.py and save the model.

5) Run app.py (If it successively trained the model and saved the model in the same folder).